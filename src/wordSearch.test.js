const test = require('ava');

/**
 * 接受一个二维字典和一个单词，如果组成这个单词的字母可以在字典中顺序找到，
 * 返回 true，否则返回 false
 * 思路：回溯算法
 * @param {character[][]} board
 * @param {string} word
 * @return {boolean}
 */
var exist = function(board, word) {
    
};
